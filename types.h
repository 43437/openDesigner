#ifndef TYPES_H
#define TYPES_H

#define OBJ_MIN_SIZE    30
typedef enum EEditMode{eSelect, eObject, eLine}EEditMode;

typedef enum EBUTTON{eNoBtn=-1, eLeftBtn, eRightBtn }EBUTTON;

#endif // TYPES_H
